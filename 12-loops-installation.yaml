- name : installation of multiple packages at once 
  hosts : local
  become: yes
  tasks: 
  - name : "installation of {{ item }}"
    ansible.builtin.dnf :
      name: "{{ item }}"
      state: installed
    loops:
    - nginx
    - httpd
    - zip
    - git
    - vim